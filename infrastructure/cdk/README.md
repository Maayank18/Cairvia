# Infrastructure

Phase 1 runs entirely on the local machine (SQLite + local HTTP API).

Later phases add Cognito, API Gateway, Lambda, DynamoDB, EventBridge, Step Functions, Bedrock, and AgentCore. This folder is reserved for that CDK app.
