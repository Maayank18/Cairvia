# Chrome extension

Phase 1 does not collect browser context.

This Manifest V3 stub exists so the monorepo shape matches the product architecture. Do not add content scripts until the Orb + Work Thread loop is stable.
